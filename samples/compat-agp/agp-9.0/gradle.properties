# Copyright (C) 2025 Rodrigo Sicarelli
# SPDX-License-Identifier: Apache-2.0

org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true

kotlin.code.style=official

android.useAndroidX=true
android.nonTransitiveRClass=true

# NOTE: On AGP 9.0+ Kotlin compilation of the testFixtures source set is the default, so
# `android.experimental.enableTestFixturesKotlinSupport` is intentionally NOT set here — this cell
# validates the flag-free AGP 9.0 path.
